GEMINI_API_KEY=
TELEGRAM_BOT_TOKEN=
OLLAMA_ENDPOINT=
OLLAMA_MODEL=
OLLAMA_API_KEY=

# Telegram (MTProto user client) — optional development convenience.
# Create at https://my.telegram.org -> API development tools.
# These are build-time values baked into the bundle; prefer entering them in
# the app's Telegram tab, which stores them on the device instead.
VITE_TELEGRAM_API_ID=
VITE_TELEGRAM_API_HASH=
